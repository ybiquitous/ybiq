name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  release:
    uses: ybiquitous/.github/.github/workflows/nodejs-release-reusable.yml@ddc048ffa862327fab4b52fedc659bd7dd4e5506 # v2.1.4
    permissions:
      contents: write
      id-token: write
