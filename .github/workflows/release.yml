name: Release

on:
  push:
    tags: ["**"]

jobs:
  release:
    uses: ybiquitous/.github/.github/workflows/nodejs-release-reusable.yml@main
    permissions:
      contents: write
      id-token: write
