name: Test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["**"]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        node-version: ["14", "16", "18"]
    uses: ybiquitous/.github/.github/workflows/test-reusable.yml@main

  lint:
    uses: ybiquitous/.github/.github/workflows/lint-reusable.yml@main

  commitlint:
    uses: ybiquitous/.github/.github/workflows/lint-reusable.yml@main
