language: node_js

cache:
  directories:
    - node_modules

script:
  - npm run lint
  - npm test
  - npm run test:coverage

deploy:
  provider: npm
  email: ybiquitous@gmail.com
  api_key: $NPM_TOKEN
  on:
    tags: true

notifications:
  email: false
